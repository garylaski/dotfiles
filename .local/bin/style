#!/bin/sh

# Check style or format file based on extension.

file=$(readlink -f "$1")
dir=${file%/*}
base="${file%.*}"
ext="${file##*.}"

cd "$dir" || exit 1

case "$ext" in
        java) java -jar $CLASSPATH/checkstyle-9.2.1-all.jar -c $CLASSPATH/checkstyle.xml $file;;
        *) exit 1 ;;
esac
